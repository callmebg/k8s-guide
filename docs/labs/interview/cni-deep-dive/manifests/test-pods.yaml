apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: cni-test
spec:
  selector:
    matchLabels:
      app: cni-test
  template:
    metadata:
      labels:
        app: cni-test
    spec:
      containers:
      - name: test
        image: busybox
        command: ["sh", "-c", "sleep 3600"]
